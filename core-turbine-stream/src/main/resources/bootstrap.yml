spring:
  application:
    name: core-turbine-stream
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: core-config
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user